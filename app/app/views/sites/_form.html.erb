<%= form_with(model: site, local: true) do |form| %>
  <% if site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :site_type_id %>
    <%= form.number_field :site_type_id %>
  </div>

  <div class="field">
    <%= form.label :favicon_id %>
    <%= form.number_field :favicon_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
