<%= form_with(model: movie_type, local: true) do |form| %>
  <% if movie_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_type.errors.count, "error") %> prohibited this movie_type from being saved:</h2>

      <ul>
      <% movie_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
